{
  "version": 2,
  "framework": null,
  "buildCommand": "mkdir -p dist && cp index.html dist/index.html && cp favicon.png dist/ && cp icon-*.png dist/ && cp shani-finance.html dist/ 2>/dev/null; true",
  "outputDirectory": "dist",
  "installCommand": "echo skip",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
